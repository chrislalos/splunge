[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "splunge-tests"
version = "0.1.0"
dependencies = [
    "splunge",
    "pytest",
]

[project.scripts]
# test-hello = "pytest:console_main"
# test-unit  = "pytest:console_main"
# test-e2e   = "pytest:console_main"

[project.optional-dependencies]
# e2e = ["httpx", "requests"]

[project.urls]
# repository = "https://github.com/..."
# documentation = "https://..."

[tool.setuptools.packages.find]
where = ["."]

[tool.pytest.ini_options]
# testpaths = ["."]
# markers   = ["slow: marks tests as slow"]
# addopts   = "--tb=short --strict-markers"
