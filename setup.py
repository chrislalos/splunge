import setuptools


setuptools.setup(name='splunge',
                 version='0.1.0',
                 scripts=['scripts/www'],
                 entry_points={}
                 )
#                 packages=setuptools.find_packages(exclude=['tests']))
